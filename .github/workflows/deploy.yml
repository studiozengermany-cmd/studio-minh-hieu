name: Deploy to GitHub Pages

on:
  workflow_dispatch:

# Disabled - project now deploys via Vercel
# Vercel auto-deploys on push to main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "GitHub Pages deployment disabled. Project deploys via Vercel."
